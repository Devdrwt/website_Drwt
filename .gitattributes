# Normalize text files to LF in the repo.
* text=auto eol=lf

# Shell scripts must keep LF endings or the container shebang breaks.
*.sh text eol=lf

# Windows-specific scripts keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
